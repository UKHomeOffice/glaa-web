@{
    ViewBag.Title = "Organisation - Part 12";
}

@model GLAA.ViewModels.LicenceApplication.ShellfishWorkerNumberViewModel

@Html.Partial("_BackButton", new ViewDataDictionary(ViewData) { { "section", "Organisation" }, { "submittedPageId", "13" } })

@using (Html.BeginForm("SaveShellfishWorkerNumber", "Organisation"))
{
    <div class="grid-row">
        <div class="column-two-thirds">
            @Html.Partial("_GdsValidationSummary")
        </div>
        <div class="column-full">
            <h1 class="heading-large">
                What is the average number of workers that you will use to gather shellfish?
            </h1>
        </div>
        <div class="column-two-thirds">
            @Html.TextFormGroupFor(x => x.NumberOfWorkers)
        </div>
    </div>
    <button type="submit" class="button">Save and continue</button>
}

@section scripts
    {
    <script>
    </script>
}