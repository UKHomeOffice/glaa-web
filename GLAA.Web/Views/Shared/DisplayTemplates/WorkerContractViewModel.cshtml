@model GLAA.ViewModels.LicenceApplication.WorkerContractViewModel

@{
    var text = Model.AvailableContracts.FirstOrDefault(m => Model.SelectedContract.HasValue && m.EnumMappedTo == Model.SelectedContract)?.Name;
}
@text