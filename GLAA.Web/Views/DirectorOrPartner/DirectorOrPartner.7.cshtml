@{
    ViewBag.Title = "Director or Partner - Part 6";
}

@model GLAA.ViewModels.LicenceApplication.JobTitleViewModel

@Html.Partial("_BackButton", new ViewDataDictionary(ViewData) { { "section", "DirectorOrPartner" }, { "submittedPageId", "7" } })

@using (Html.BeginForm("SaveJobTitle", "DirectorOrPartner"))
{
    <div class="grid-row">
        <div class="column-two-thirds">
            @Html.Partial("_GdsValidationSummary")
        </div>
        <div class="column-full">
            <h1 class="heading-large">
                What is your job title?
            </h1>
        </div>
        <div class="column-two-thirds">
            @Html.TextFormGroupFor(x => x.JobTitle)
        </div>
    </div>

    <button type="submit" class="button">Save and continue</button>
}